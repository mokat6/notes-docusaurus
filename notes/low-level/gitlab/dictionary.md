Continuous Integration - Integrating code changes frequently. Merge
Continuous Delivery - Automating release process. Release artifacts
Continuous Deployment - Automatically deploying to production. Bump up, new artifact is deployed.

C delivery - pipeline's last stage `deploy` is manual.
C deployment - all pipeline stages automatic.

Smoke test - initial test, most basic and critical functions of a system are working

```bash
curl 'https://trainin-pipes.netlify.app/' | grep 'Gitlab'
curl -s  https://valentinos-magic-beans.click/ | grep -iq "valentxxx"
```

-s silent  
-q quiet. Same as silent, no output. check success/fail via `echo $?`

Origins. In electronics engineering, when a circuit is developped, engineed would apply current. If there's smoke, means some short circuit somewhere.

review environment - temp env, each MR gets its own temp deployment.
